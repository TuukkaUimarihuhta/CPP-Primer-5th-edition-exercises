#include <iostream>
#include <string>

//an array with 10 strings, all empty
std::string sa[10];
//array of ten ints, all 0
int ia[10];

int e3_28() {

	//an array with 10 strings, all empty
	std::string sa2[10];
	//array of ten ints. because this is inside a function, the values are undefined
	int ia2[10];

	return 0;
}